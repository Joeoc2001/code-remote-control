{
  "name": "code-remote-control",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/container-metadata-types",
    "packages/container-metadata-server",
    "packages/client",
    "packages/server"
  ],
  "scripts": {
    "dev:client": "npm run dev -w packages/client",
    "dev:server": "npm run dev -w packages/server",
    "build": "npm run build -w packages/client && npm run build -w packages/server",
    "typecheck": "npm run typecheck -w packages/container-metadata-types && npm run typecheck -w packages/container-metadata-server && npm run typecheck -w packages/client && npm run typecheck -w packages/server",
    "check:opencode-assets": "node --check opencode/plugins/*.js && node --experimental-strip-types --check opencode/tools/*.ts"
  }
}
